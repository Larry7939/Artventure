<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Artventure" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:includeFontPadding">false</item>
         <item name="colorPrimary">@color/G1</item>
    </style>

    <style name="Widget.NoRippleBottomNavigationView" parent="Widget.Design.BottomNavigationView">
        <item name="android:background">@color/G6</item>
    </style>

    <style name="Theme.App.Splash" parent="Theme.SplashScreen">
        ....
        <item name="android:windowSplashScreenBackground" tools:targetApi="S">@color/white</item>
        <item name="android:windowSplashScreenAnimatedIcon" tools:targetApi="S">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.Artventure</item>
    </style>
</resources>